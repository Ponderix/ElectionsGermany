//to get the data from the csv file
/*d3.csv(["/map_data/wk17_prcsd", function(data) {
    for (var i = 0; i < data.length; i++) {
        console.log(data[i].Name);
        console.log(data[i].Age);
    }
}]);*/

//svg pan zoom
var panZoom = svgPanZoom('#WahlkreiseMap');
